/**
 * represents the possible types of a move action,
 * e.g. normal shift, attack jump, invalid / impossible
 */
public enum MoveType {
    NONE, NORMAL, KILL//TODO attach strings to these, make NONE say why invalid move ;
}
